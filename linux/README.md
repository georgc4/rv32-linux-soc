# Linux build area

Reserved for pinned kernel config deltas, rootfs manifests, DTB source, build scripts and SHA256 records. No kernel source or large dependency is vendored. A baseline must be selected before creating a build recipe.
